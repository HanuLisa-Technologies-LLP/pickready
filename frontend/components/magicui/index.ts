/**
 * Magic UI components in use on this project.
 *
 * These belong on MARKETING surfaces (the landing page, the occasional hero
 * card). Never put one inside a dense data table or a form, per
 * docs/spec/DESIGN_BRIEF.md rule 5.
 *
 * Each file carries a note where it was ported from the registry's Tailwind v4
 * syntax to this project's Tailwind 3.4.
 */
export { Marquee } from "./marquee";
export { DotPattern } from "./dot-pattern";
export { BlurFade } from "./blur-fade";
