"""The assessment conversation engine (PLAN-p3 WP3).

`timers` is the pure turn clock, `pauses` the intervals it does not count,
`turns` the engine that opens a turn, takes one answer and moves on. The
package `__init__` imports nothing, so importing one module never drags in the
rest.
"""
