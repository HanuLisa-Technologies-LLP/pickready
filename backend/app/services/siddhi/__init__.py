"""Siddhi's report-writing layer.

    citations   the structural guarantee that no delivered statement is uncited

The PRISM Report's structure, header, section order and "what never appears"
list are unchanged and live where they always have --
`components/functional-skills-report.tsx`, `report_pdf.SECTION_ORDER` and
`services/report_evidence`. This package holds only what spec-doc5 §A.3 adds:
citation enforcement implemented "in code, not in a prompt".
"""
