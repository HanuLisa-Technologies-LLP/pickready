fastapi>=0.115
firebase-admin>=6.5
uvicorn[standard]>=0.30
gunicorn>=22.0
pydantic>=2.8
pydantic-settings>=2.4
SQLAlchemy[asyncio]>=2.0.30
asyncpg>=0.29
alembic>=1.13
pgvector>=0.3
# Cache, rate limiting, the proctoring warning counter and the background
# run-status record. NOT a message broker: Celery and Flower were removed on
# 2026-09-04 when background work moved to Lambda and on-demand Fargate, and
# nothing publishes to or consumes from a Redis queue any more.
redis>=5.0
# The Lambda runtime interface client. It is what makes a plain container image
# invocable as a Lambda function, and it is a REQUIREMENT rather than a base
# image because the worker and the two agent functions run the backend image
# and could not otherwise share it with the API.
awslambdaric>=3.0
PyJWT>=2.9
cryptography>=43.0
httpx>=0.27
aiosmtplib>=3.0
langchain>=0.3
# The router's retry loop is still a compiled LangGraph StateGraph. Vendor
# consolidation removed the fallback CHAIN, not the state machine.
langgraph>=0.2
# The three per-vendor LangChain adapters went with the three vendors
# (spec-doc5 Part B). The Anthropic Messages API and the Voyage embeddings API
# are both called directly over httpx: this module already owns retries,
# timeouts and backoff against a per-task budget, and an SDK running its own
# retry policy inside ours produces a worst case that is the PRODUCT of the two
# budgets rather than the sum.
# Advanced web search for the BD Portal's AI Reach agent
# (services/web_research.py). Optional at runtime: the service degrades to an
# empty "from the internet" segment when the client or TAVILY_API_KEY is absent.
tavily-python>=0.5
pypdf>=4.3
reportlab>=4.2
Pillow>=10.4
python-docx>=1.1
python-multipart>=0.0.9
# S3 object storage (spec-doc5 Part D). Credentials are never configured in
# code: boto3 resolves the ECS task role, scoped to exactly one bucket.
boto3>=1.35
email-validator>=2.2
icalendar>=5.0
structlog>=24.0
pytest>=8.3
pytest-asyncio>=0.24
# Per-package coverage floors (spec-doc6 §11.1), enforced by
# scripts/check-coverage-floors.py against the JSON this produces. Branch
# coverage is switched on in .coveragerc: a line-only figure treats a band cap
# as covered the moment either arm runs, and the arms are where the defects are.
pytest-cov>=5.0
aiosqlite>=0.20
# S3 stubbing for the storage tests. A test that needs a real bucket is a test
# that does not run in CI.
moto[s3]>=5.0
# Property-based testing for the aggregator's band caps (spec-doc6 §4.4, §11.1).
# The hard-cap invariant is a claim about EVERY set of item grades, not about
# the handful a table-driven test can enumerate, and the ordering invariant
# (delivered <= ceiling for every composite and every authenticity multiplier)
# is a claim about a continuous range. Neither is expressible as examples.
hypothesis>=6.100

# Runbook data package (RPN-PHIL-001 extraction, app/services/hiring/runbook_data/).
# Pinned EXPLICITLY rather than relied on transitively through langchain-core: the
# hiring intelligence layer fails at import without it, and a transitive dependency
# that a vendor drops in a minor release is an outage nobody predicted.
PyYAML>=6.0
# Typing for the strict-mode gate (spec-doc6 §10.3). PyYAML ships no py.typed, so
# --strict reports missing stubs without this.
types-PyYAML>=6.0
mypy>=1.11

# ── AI upgrade, RPN-AI-UP-001 ───────────────────────────────────────────────

# Voyage rerank (rerank-2.5), through the official client rather than raw httpx,
# unlike the chat models: the chat path owns retries against a per-task budget
# and an SDK inside that would multiply budgets, but rerank is a single bounded
# call on Route.ECS where that argument does not apply.
#
# 0.5, NOT the specification's >=0.3,<0.4. That pin predates the release the
# code was actually written against: `AsyncClient(api_key, max_retries,
# timeout)` and `rerank(query, documents, model, top_k, truncation)` returning
# `.results[].index` were read off the installed 0.5.0, and pinning to a range
# the verified shape does not come from is the `voyage-context-4` mistake in
# dependency form.
#
# NOT OPTIONAL EVEN FOR A `lexical` DEPLOYMENT: `services/rag/reranker.py`
# imports it at module scope, so the image fails to import without it. The full
# test suite caught exactly that.
voyageai>=0.5,<0.6

# OpenTelemetry GenAI (W4.6). The semantic conventions are still Development,
# not Stable, and the dedicated repository carries no tags, so there is no
# schema version to pin: `tests/test_otel_span_shape.py` asserts the attributes
# of a real exported span against a checked-in fixture instead, which makes a
# convention drift a failing test rather than a silently renamed field.
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
# Pinned EXPLICITLY, the PyYAML precedent: `services/observability/otel.py`
# imports `opentelemetry.semconv` directly and fails at import without it,
# while it otherwise arrives only transitively through the sdk.
opentelemetry-semantic-conventions>=0.48b0
